# Base URL for the Newell gateway API.
# Leave unset in dev to use the Vite proxy at /api -> http://localhost:8080
# (see vite.config.ts). Set an absolute URL to point at a different gateway.
VITE_GATEWAY_URL=/api
